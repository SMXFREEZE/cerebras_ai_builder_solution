{
  "name": "asset-tracking-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "Take-home challenge: lab equipment asset tracking, monorepo with hosted API and Next.js starter.",
  "license": "MIT",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "corepack pnpm -r --parallel dev",
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "lint": "corepack pnpm -r lint",
    "demo:record": "corepack pnpm --filter @assetops/demo-video record",
    "demo:render": "corepack pnpm --filter @assetops/demo-video render",
    "demo:loom": "corepack pnpm --filter @assetops/demo-video render:loom"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "ffmpeg-static",
      "sharp",
      "unrs-resolver"
    ]
  }
}
